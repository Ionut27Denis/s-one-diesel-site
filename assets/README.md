# assets/

Adaugă aici fișierul real al siglei, cu exact acest nume:

```
logo-sone-diesel.png
```

(925×417px, fundal transparent). Toate referințele din `index.html` (header, footer, favicon, JSON-LD) indică deja spre `assets/logo-sone-diesel.png`, deci pagina va prelua sigla automat, fără nicio modificare de cod.
